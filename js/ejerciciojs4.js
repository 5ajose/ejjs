const menu = {
    "pizza": 200,
    "hamburguesa": 300,
    "lasagna": 300,
    "gaseosa": 100,
    "jugo": 150
};

let total = 0;

function seleccionarProductos() (mostrarMenu ()){
    mostrarMenu();
    let seleccion;

    while (true) {
        seleccion = prompt ("Escribe el nombre del producto que quieres llevar o escribe "pagar" para finalizar la compra") toLoweCase();
        if (seleccion === "pagar"){
            break
        }

        

    }
}
for (const producto in menu) {
    if (Object.prototype.hasOwnProperty.call(object, key)) {
        const element = object[key];
        
    }
}